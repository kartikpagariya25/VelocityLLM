# Core inference engine
vllm

# API server
fastapi
uvicorn[standard]
pydantic

# Load generator / async HTTP client
aiohttp

# Hugging Face model download and auth
huggingface_hub

# --- Phase 3 (Robustness & Testing) — install when reached ---
# pytest
# pytest-asyncio
# httpx

# --- Phase 4 (Metrics/Observability) — install when reached ---
# prometheus_client

# --- Phase 5 (Dashboard) — install when reached ---
# streamlit
# pandas
